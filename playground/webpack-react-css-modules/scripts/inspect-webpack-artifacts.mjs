import fs from 'node:fs/promises';import path from 'node:path';import {fileURLToPath} from 'node:url';const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..');const semantic=path.join(root,'dist/semantic');const native=path.join(root,'dist/native');for(const file of ['index.html','inspector.html']){const html=await fs.readFile(path.join(semantic,file),'utf8');if(!html.includes('/static/css/semantic-atomic.css'))throw new Error(`${file}: atomic link missing`);if(html.indexOf('/static/css/semantic-atomic.css')>html.indexOf('/static/css/'))throw new Error(`${file}: atomic link order`);}for(const file of ['semantic-atomic-manifest.json','semantic-atomic-report.json'])await fs.access(path.join(semantic,file));await fs.access(path.join(native,'index.html'));console.log('Webpack React Pilot artifacts passed.');
